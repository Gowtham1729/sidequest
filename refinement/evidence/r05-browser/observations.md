# R05 browser evidence — 8 September 2026

Source fingerprint: `9e4776ae3af5fcc4a0b7c000f8af42b9fabc5b25742bf2fb31d6f521154522f4`. Chrome DevTools, local `http://localhost:8901/`, mobile/touch emulation. These are desktop browser checks, not physical-device evidence.

- Matching preview board: retained the baseline ES module in browser memory before editing; mounted baseline and candidate successively over the same 320×568 viewport. Baseline tiles visibly stretched vertically. Candidate square tiles have even gaps, legible numerals and centered composition. Screenshots inspected in conversation.
- Actual shell at 390×844: top HUD 93px, bottom HUD 97px; board 354×354 CSS px, cells 82.5×82.5, gaps 8px. At 320×568: board 284×284, cells 65×65, gaps 8px. No horizontal overflow in either viewport. Screenshots inspected in active play; rightmost tile centers remain inside the gameplay area.
- Actual input: two synthetic touch swipes through arena pointer events made a merge (score 4), stayed in 2048, and accepted subsequent input. Sixteen rapid alternating keyboard events dispatched to document resolved without an unbounded queue (score 8). An earlier window-targeted keyboard experiment did not reach the app's document listener and is not counted as input evidence.
- Keyboard pause and Space resume worked. Active board remained correctly laid out after changing from 390×844 to 320×568. No console errors in the inspected app tab.
- Test-only browser fixture: fetched candidate source and replaced initial spawn statements in an in-memory Blob module with a deterministic full board beginning `[1024,1024,8,16]`. No debug setter or fixture was added to shipped code. Invoked left, inspected travel at 60ms and settled outcome at 380ms. Moving 1024 sources and final 2048 remained legible at 320×568. Score 2048 and one win event with 400ms duration were recorded. Fixture used stub audio/outcome callbacks, so this establishes game rendering, not actual shell overlay timing. Fixture was destroyed and removed afterward.
- Reference inspection: https://play2048.co/ (Gabriele Cirulli attribution visible) loaded after an initial navigation timeout. Inspected its rendered board: square cells, consistent gaps, large centered numerals, muted empty cells, a bounded board surface and shallow tile depth. Adopted comparable legibility/composition principles; did not time its animation or claim gameplay parity. Inspected after first implementation, so this was a comparison check rather than pre-implementation evidence.

Artifact limitation: the connector denied the requested screenshot save path as outside its configured workspace roots. Inline screenshots were available and inspected; no saved image files are claimed. Full independent visual review, reduced-motion browser capture, physical swipe feel, audio listening and performance traces remain open. Code tests separately cover reduced motion, pause, cancellation, terminal loss/win, resize and cleanup.
